<h1>Funcionarios</h1>

<form action="/funcionarios/{{#if action}}save{{else}}create{{/if}}" method="post">
    {{!-- <input type="hidden" name="id" value="{{fornecedor_editing.id}}"> --}}

    <label for="nome">Nome</label><br>
    <input type="text" id="nome" name="nome" value="{{fornecedor_editing.nome}}" required><br><br>

    <label for="email">Email</label><br>
    <input type="text" id="email" name="email" value="{{fornecedor_editing.email}}" required><br><br>

    <label for="endereco">Endereço</label><br>
    <input type="text" id="endereco" name="endereco" value="{{fornecedor_editing.endereco}}" required><br><br>

    <label for="cargo">Cargo</label><br>
    <input type="text" id="cargo" name="cargo" value="{{fornecedor_editing.cargo}}" required><br><br>

    <label for="data_nascimento">Data de Nascimento</label><br>
    <input type="date" id="data_nascimento" name="data_nascimento" value="{{produto_editing.data_nascimento}}" required><br><br>


    <button>{{#if action}}Editar{{else}}Criar{{/if}}</button>
</form>

<hr>

<table>
    <thead>
        <tr>
            <th>Nome</th>
            <th>Email</th>
            <th>Endereço</th>
            <th>Cargo</th>
            <th>Data de Nascimento</th>
        </tr>
    </thead>
    <tbody>
        {{#each funcionarios}}
        <tr>
            <td>{{this.nome}}</td>
            <td>{{this.email}}</td>
            <td>{{this.endereco}}</td>
            <td>{{this.cargo}}</td>
            <td>{{this.data_nascimento}}</td>
            <td style="display: flex; justify-content: space-evenly;">
                <form action="/funcionarios/edit" method="post">
                    <input type="hidden" name="id" value="{{this.id}}">
                    <button type="submit">Editar</button>
                </form>

                <form action="/funcionarios/delete" method="post">
                    <input type="hidden" name="id" value="{{this.id}}">
                    <button type="submit">Excluir</button>
                </form>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>

